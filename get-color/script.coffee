square = (x) -> x * x
console.log square 5
# Ensure that the DOM is ready
$ ->
  # Select the element with the ID 'link'
  linkElement = $('#link')

  # Log the selected element to the console
  console.log linkElement
